This program takes an BMP (bitmap) image file, and based on the user's argument, it will output the image with a filter as a new output image.
The supported filters are: Grayscale (black and white), Blur, Mirror and Edge Detection. 
